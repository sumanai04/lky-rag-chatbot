# Chat examples

Real transcripts from the LKY RAG chatbot. Generated by running the retrieval pipeline and the DeepSeek model configured in `.env`.

Each entry shows the question, the answer, and the sources the answer was grounded on.

<!-- Examples are appended after running: python rag.py ask "..." -->
